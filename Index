<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>MCAS Quest</title>
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: Arial, Helvetica, sans-serif;
      color: white;
      background: linear-gradient(135deg, #020617, #312e81, #020617);
    }

    .page {
      max-width: 1100px;
      margin: 0 auto;
      padding: 24px;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 20px;
      margin-bottom: 24px;
      flex-wrap: wrap;
    }

    .eyebrow {
      color: #c7d2fe;
      text-transform: uppercase;
      letter-spacing: 0.15em;
      font-size: 14px;
      margin-bottom: 8px;
    }

    h1 {
      font-size: clamp(42px, 8vw, 72px);
      margin: 0;
      line-height: 0.95;
    }

    h2 {
      margin-top: 0;
    }

    .stats {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }

    .stat {
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.15);
      border-radius: 18px;
      padding: 10px 14px;
      font-weight: 700;
    }

    .card {
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.15);
      border-radius: 28px;
      padding: 28px;
      box-shadow: 0 24px 80px rgba(0, 0, 0, 0.35);
    }

    .start-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 28px;
      align-items: center;
    }

    .mission-card {
      background: linear-gradient(135deg, #6366f1, #d946ef);
      border-radius: 28px;
      padding: 24px;
    }

    .mission-inner {
      background: rgba(15, 23, 42, 0.5);
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 24px;
      padding: 24px;
    }

    .play-grid {
      display: grid;
      grid-template-columns: 1fr 1.2fr;
      gap: 24px;
    }

    .story-card {
      min-height: 300px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      background: rgba(15, 23, 42, 0.55);
      border-radius: 24px;
      padding: 24px;
      border: 1px solid rgba(255, 255, 255, 0.15);
      animation: popIn 0.25s ease-out;
    }

    .question-card {
      background: white;
      color: #020617;
    }

    .progress-track {
      height: 12px;
      background: rgba(255, 255, 255, 0.12);
      border-radius: 999px;
      overflow: hidden;
      margin-bottom: 8px;
    }

    .progress-bar {
      height: 100%;
      width: 0%;
      background: white;
      border-radius: 999px;
      transition: width 0.25s ease;
    }

    .small-text {
      color: #c7d2fe;
      font-size: 14px;
    }

    .emoji {
      font-size: 80px;
      text-align: center;
      padding: 30px 0;
    }

    .tags {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 16px;
    }

    .tag {
      border-radius: 999px;
      padding: 8px 12px;
      background: #e0e7ff;
      color: #312e81;
      font-size: 14px;
      font-weight: 700;
    }

    .tag.gray {
      background: #f1f5f9;
      color: #334155;
    }

    .question-text {
      font-size: clamp(24px, 4vw, 34px);
      font-weight: 900;
      margin: 0 0 24px;
    }

    .choices {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
      margin-bottom: 20px;
    }

    .choice {
      border: 2px solid #e2e8f0;
      background: #f8fafc;
      color: #020617;
      border-radius: 18px;
      padding: 18px;
      font-size: 18px;
      font-weight: 800;
      text-align: left;
      cursor: pointer;
      transition: 0.15s ease;
    }

    .choice:hover:not(:disabled) {
      border-color: #6366f1;
      background: #eef2ff;
      transform: translateY(-1px);
    }

    .choice:disabled {
      cursor: not-allowed;
    }

    .choice.correct {
      background: #dcfce7;
      border-color: #22c55e;
    }

    .choice.wrong {
      background: #ffe4e6;
      border-color: #f43f5e;
    }

    .feedback {
      display: none;
      border-radius: 18px;
      padding: 16px;
      margin-bottom: 18px;
      line-height: 1.4;
    }

    .feedback.correct {
      display: block;
      background: #dcfce7;
    }

    .feedback.wrong {
      display: block;
      background: #ffe4e6;
    }

    button.main-button {
      border: none;
      border-radius: 18px;
      padding: 18px 26px;
      font-size: 18px;
      font-weight: 900;
      cursor: pointer;
      background: #4f46e5;
      color: white;
      transition: 0.15s ease;
    }

    button.main-button:hover {
      background: #4338ca;
      transform: translateY(-1px);
    }

    .full-width {
      width: 100%;
    }

    .center {
      text-align: center;
    }

    .hidden {
      display: none !important;
    }

    @keyframes popIn {
      from {
        opacity: 0;
        transform: translateY(16px) scale(0.98);
      }
      to {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }

    @media (max-width: 850px) {
      .start-grid,
      .play-grid,
      .choices {
        grid-template-columns: 1fr;
      }

      .page {
        padding: 16px;
      }

      .card {
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="page">
    <header>
      <div>
        <div class="eyebrow">🎮 7th Grade MCAS Review</div>
        <h1 id="title">MCAS Quest</h1>
      </div>

      <div id="stats" class="stats hidden">
        <div class="stat">❤️ Lives: <span id="lives">3</span></div>
        <div class="stat">🪙 Coins: <span id="coins">0</span></div>
        <div class="stat">🔥 Streak: <span id="streak">0</span></div>
        <div class="stat">⭐ Level: <span id="level">1</span></div>
        <div class="stat">🛡️ Shield: <span id="shield">None</span></div>
      </div>
    </header>

    <main>
      <section id="startScreen" class="card start-grid">
        <div>
          <h2>Turn MCAS practice into a solo quest.</h2>
          <p>
            Students run through an endless MCAS world on their Chromebook. Questions appear as traps, boss battles, and gates. Correct answers build streaks, earn coins, and unlock shields.
          </p>
          <button class="main-button" onclick="startGame()">Start Quest</button>
        </div>
        <div class="mission-card">
          <div class="mission-inner">
            <div class="emoji">🛡️</div>
            <h2>Mission:</h2>
            <p>Survive as long as possible, build streaks, and beat your high score.</p>
          </div>
        </div>
      </section>

      <section id="playScreen" class="play-grid hidden">
        <div class="card">
          <div class="progress-track">
            <div id="progressBar" class="progress-bar"></div>
          </div>
          <p id="levelInfo" class="small-text"></p>

          <div id="storyCard" class="story-card">
            <div>
              <p class="small-text">Story</p>
              <h2 id="storyText"></h2>
              <div id="storyEmoji" class="emoji"></div>
            </div>
            <p>Answer the math gate to keep going.</p>
          </div>
        </div>

        <div class="card question-card">
          <div class="tags">
            <span id="categoryTag" class="tag"></span>
            <span id="difficultyTag" class="tag gray"></span>
          </div>

          <p id="questionText" class="question-text"></p>
          <div id="choices" class="choices"></div>

          <div id="feedback" class="feedback"></div>
          <button id="nextButton" class="main-button full-width hidden" onclick="nextQuestion()">Next Level</button>
        </div>
      </section>

      <section id="gameOverScreen" class="card question-card center hidden">
        <h2 style="font-size: 44px;">You ran out of lives.</h2>
        <p style="font-size: 22px;">Coins collected: <span id="finalCoins">0</span></p>
        <p style="font-size: 20px;">Final level reached: <span id="finalLevel">1</span></p>
        <p>Try again and use the explanations to beat more gates.</p>
        <button class="main-button" onclick="startGame()">↻ Restart Quest</button>
      </section>
    </main>
  </div>

  <script>
    const endlessQuestions = [
      {
        category: "Ratios & Proportional Relationships",
        difficulty: "Warm-Up Gate",
        prompt: "A recipe uses 3 cups of flour for every 2 cups of sugar. If the baker uses 12 cups of flour, how many cups of sugar should be used?",
        choices: ["6", "8", "10", "18"],
        answer: "8",
        explanation: "3 cups of flour becomes 12 cups by multiplying by 4, so 2 cups of sugar also gets multiplied by 4. 2 × 4 = 8.",
        bonus: 100
      },
      {
        category: "Number System",
        difficulty: "Integer Bridge",
        prompt: "What is the value of -7 + 12 - 4?",
        choices: ["-23", "-1", "1", "9"],
        answer: "1",
        explanation: "-7 + 12 = 5, and 5 - 4 = 1.",
        bonus: 100
      },
      {
        category: "Expressions & Equations",
        difficulty: "Equation Lock",
        prompt: "Solve for x: 4x - 7 = 21",
        choices: ["x = 3.5", "x = 7", "x = 14", "x = 28"],
        answer: "x = 7",
        explanation: "Add 7 to both sides to get 4x = 28. Then divide by 4, so x = 7.",
        bonus: 150
      },
      {
        category: "Geometry",
        difficulty: "Angle Door",
        prompt: "Two angles form a straight line. One angle measures 63°. What is the measure of the other angle?",
        choices: ["27°", "63°", "117°", "180°"],
        answer: "117°",
        explanation: "Angles on a straight line add to 180°. 180 - 63 = 117°.",
        bonus: 150
      },
      {
        category: "Statistics & Probability",
        difficulty: "Probability Portal",
        prompt: "A bag has 5 red tiles, 3 blue tiles, and 2 green tiles. What is the probability of choosing a blue tile?",
        choices: ["3/10", "2/10", "3/7", "5/10"],
        answer: "3/10",
        explanation: "There are 10 total tiles and 3 are blue, so the probability is 3/10.",
        bonus: 150
      },
      {
        category: "Ratios & Proportional Relationships",
        difficulty: "Percent Cave",
        prompt: "A sweatshirt costs $40. It is on sale for 25% off. What is the sale price?",
        choices: ["$10", "$15", "$30", "$35"],
        answer: "$30",
        explanation: "25% of $40 is $10. Subtract the discount: $40 - $10 = $30.",
        bonus: 200
      },
      {
        category: "Expressions & Equations",
        difficulty: "Inequality Trap",
        prompt: "Which value makes the inequality x + 6 < 10 true?",
        choices: ["x = 5", "x = 4", "x = 3", "x = 10"],
        answer: "x = 3",
        explanation: "If x = 3, then 3 + 6 = 9, and 9 < 10 is true.",
        bonus: 200
      }
    ];

    const scenes = [
      "You enter the MCAS Training Arena. A glowing gate blocks your path.",
      "You cross the Integer Bridge, but the floor starts disappearing.",
      "A locked door appears with an equation carved into it.",
      "You step into the Geometry Temple. The walls are covered in angles.",
      "A portal opens, but it only accepts probability power.",
      "You find a treasure cave, but the price tags are enchanted.",
      "You sprint through the Percent Cave while glowing price tags fly past you.",
      "A tricky inequality trap snaps shut around your feet."
    ];

    const sceneEmoji = ["🏰", "🌉", "🚪", "🏛️", "🌀", "💎", "🏷️", "⚡"];

    let screen = "start";
    let index = 0;
    let level = 1;
    let streak = 0;
    let shield = false;
    let lives = 3;
    let coins = 0;
    let selected = null;
    let answered = false;
    let current = getRandomQuestion();

    function calculateEarnedCoins(questionBonus, currentLives, isBossBattle = false) {
      const bossMultiplier = isBossBattle ? 2 : 1;
      return (questionBonus + currentLives * 25) * bossMultiplier;
    }

    function getRandomQuestion(questionBank = endlessQuestions) {
      const randomIndex = Math.floor(Math.random() * questionBank.length);
      return questionBank[randomIndex];
    }

    function validateQuestionBank(questionBank) {
      return Array.isArray(questionBank) &&
        questionBank.length > 0 &&
        questionBank.every((question) => {
          return question &&
            typeof question.category === "string" &&
            question.category.length > 0 &&
            typeof question.difficulty === "string" &&
            question.difficulty.length > 0 &&
            typeof question.prompt === "string" &&
            question.prompt.length > 0 &&
            Array.isArray(question.choices) &&
            question.choices.length >= 2 &&
            question.choices.every((choice) => typeof choice === "string" && choice.length > 0) &&
            typeof question.answer === "string" &&
            question.choices.includes(question.answer) &&
            typeof question.explanation === "string" &&
            question.explanation.length > 0 &&
            typeof question.bonus === "number" &&
            question.bonus > 0;
        });
    }

    function getLevelProgress(currentLevel) {
      return ((currentLevel - 1) % 10) * 10;
    }

    function shouldAwardShield(nextStreak) {
      return nextStreak > 0 && nextStreak % 5 === 0;
    }

    function isBossLevel(currentLevel) {
      return currentLevel > 0 && currentLevel % 10 === 0;
    }

    // Lightweight test cases. These do not affect gameplay, but they help catch broken question data.
    console.assert(validateQuestionBank(endlessQuestions), "Every question must have choices, an answer, an explanation, and a positive bonus.");
    console.assert(scenes.length > 0, "There should be at least one story scene.");
    console.assert(sceneEmoji.length > 0, "There should be at least one scene emoji.");
    console.assert(calculateEarnedCoins(100, 3) === 175, "Coin formula should add the bonus plus 25 coins per remaining life.");
    console.assert(calculateEarnedCoins(200, 1) === 225, "Coin formula should work when only 1 life remains.");
    console.assert(calculateEarnedCoins(100, 3, true) === 350, "Boss battles should double earned coins.");
    console.assert(getLevelProgress(1) === 0, "Level 1 progress should start at 0%.");
    console.assert(getLevelProgress(10) === 90, "Level 10 should show 90% progress toward the boss cycle.");
    console.assert(getLevelProgress(11) === 0, "Level 11 should restart the progress cycle.");
    console.assert(shouldAwardShield(5) === true, "A 5-answer streak should award a shield.");
    console.assert(shouldAwardShield(4) === false, "A 4-answer streak should not award a shield yet.");
    console.assert(isBossLevel(10) === true, "Level 10 should be a boss level.");
    console.assert(isBossLevel(9) === false, "Level 9 should not be a boss level.");
    console.assert(endlessQuestions.every((question) => question.choices.includes(question.answer)), "Each answer must appear in its choices.");

    function startGame() {
      screen = "play";
      index = 0;
      level = 1;
      streak = 0;
      shield = false;
      lives = 3;
      coins = 0;
      selected = null;
      answered = false;
      current = getRandomQuestion();
      render();
    }

    function checkAnswer(choice) {
      if (answered) return;

      selected = choice;
      answered = true;
      const bossBattle = isBossLevel(level);
      const feedback = document.getElementById("feedback");

      if (choice === current.answer) {
        const nextStreak = streak + 1;
        const earned = calculateEarnedCoins(current.bonus, lives, bossBattle);
        const earnedShield = shouldAwardShield(nextStreak);

        streak = nextStreak;
        coins += earned;

        if (earnedShield) {
          shield = true;
        }

        feedback.className = "feedback correct";
        feedback.innerHTML = `<strong>${bossBattle ? "Boss defeated!" : "Correct!"}</strong> You earned ${earned} coins.${earnedShield ? " You unlocked a shield." : ""}<br><br>${current.explanation}`;
      } else {
        const shieldProtected = shield;
        const newLives = shieldProtected ? lives : lives - 1;

        shield = false;
        streak = 0;
        lives = newLives;

        feedback.className = "feedback wrong";
        feedback.innerHTML = `<strong>Not quite.</strong> The correct answer was ${current.answer}. ${shieldProtected ? "Your shield protected you, so you did not lose a life." : "You lost 1 life."}<br><br>${current.explanation}`;

        if (lives <= 0) {
          setTimeout(() => {
            screen = "gameover";
            render();
          }, 1200);
        }
      }

      renderChoices();
      updateStats();
      document.getElementById("nextButton").classList.toggle("hidden", lives <= 0);
    }

    function nextQuestion() {
      index += 1;
      level += 1;
      current = getRandomQuestion();
      selected = null;
      answered = false;
      render();
    }

    function updateStats() {
      document.getElementById("lives").textContent = lives;
      document.getElementById("coins").textContent = coins;
      document.getElementById("streak").textContent = streak;
      document.getElementById("level").textContent = level;
      document.getElementById("shield").textContent = shield ? "Ready" : "None";
    }

    function renderChoices() {
      const choicesContainer = document.getElementById("choices");
      choicesContainer.innerHTML = "";

      current.choices.forEach((choice) => {
        const button = document.createElement("button");
        button.className = "choice";
        button.textContent = choice;
        button.disabled = answered;
        button.onclick = () => checkAnswer(choice);

        if (answered && choice === current.answer) {
          button.classList.add("correct");
        }

        if (answered && selected === choice && choice !== current.answer) {
          button.classList.add("wrong");
        }

        choicesContainer.appendChild(button);
      });
    }

    function render() {
      const bossBattle = isBossLevel(level);
      const progress = getLevelProgress(level);

      document.getElementById("title").textContent = screen === "gameover" ? "Game Over" : "MCAS Quest";
      document.getElementById("stats").classList.toggle("hidden", screen !== "play");
      document.getElementById("startScreen").classList.toggle("hidden", screen !== "start");
      document.getElementById("playScreen").classList.toggle("hidden", screen !== "play");
      document.getElementById("gameOverScreen").classList.toggle("hidden", screen !== "gameover");

      if (screen === "play") {
        updateStats();
        document.getElementById("progressBar").style.width = progress + "%";
        document.getElementById("levelInfo").textContent = bossBattle
          ? `Level ${level} • Boss Battle`
          : `Level ${level} • Next boss at level ${Math.ceil(level / 10) * 10}`;
        document.getElementById("storyText").textContent = bossBattle
          ? "⚠️ BOSS BATTLE! A giant MCAS guardian blocks your path. Correct answers are worth double coins."
          : scenes[index % scenes.length];
        document.getElementById("storyEmoji").textContent = bossBattle ? "👹" : sceneEmoji[index % sceneEmoji.length];
        document.getElementById("categoryTag").textContent = current.category;
        document.getElementById("difficultyTag").textContent = bossBattle ? "Boss Battle" : current.difficulty;
        document.getElementById("questionText").textContent = current.prompt;
        document.getElementById("feedback").className = "feedback";
        document.getElementById("feedback").innerHTML = "";
        document.getElementById("nextButton").classList.add("hidden");
        renderChoices();
      }

      if (screen === "gameover") {
        document.getElementById("finalCoins").textContent = coins;
        document.getElementById("finalLevel").textContent = level;
      }
    }

    render();
  </script>
</body>
</html>
